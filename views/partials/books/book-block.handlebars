<li>
	{{dataValues.title}}
    {{dataValues.author}}
	{{!-- <button class="change-haveRead" data-id="{{dataValues.id}}" data-newRead="{{dataValues.read}}">
		{{#if read}}Read Book!{{else}}Reread!{{/if}}
	</button> --}}

	<button class="delete-book" data-id="{{dataValues.id}}">DELETE!</button>
</li>